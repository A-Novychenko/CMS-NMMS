const config = {
  locales: [
    // 'de',
    // 'es',
    // 'it',
    // 'ja',
    // 'nl',
    // 'ru',
    // 'sk',
    // 'uk',
  ],
};

const bootstrap = (app) => {
  console.log(app);
};

export default {
  config,
  bootstrap,
};
