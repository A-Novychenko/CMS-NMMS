const config = {
  locales: [
    // 'de',
    // 'it',
    // 'ja',
    // 'ru',
    // 'sk',
    // 'uk',
  ],
};

const bootstrap = (app) => {
  console.log(app);
};

export default {
  config,
  bootstrap,
};
